{
  "name": "sisyphus",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:mobile": "tsc && vite build --config vite.mobile.config.ts",
    "build:sisyphus": "tsc && vite build --config vite.sisyphus.config.ts",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
